name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  unit-tests:
    name: Testes unitários (Vitest)
    timeout-minutes: 15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Sem `version`: a action lê `packageManager` do package.json, então CI e
      # local usam exatamente a mesma pnpm. Com `version: 11` (flutuante) o CI
      # quebrou sozinho quando o pnpm 11.17 passou a falhar em builds ignorados.
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Lint de tipos e build
        run: pnpm build
      - name: Run unit tests
        run: pnpm test

  e2e-tests:
    name: Testes E2E (Playwright)
    timeout-minutes: 30
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Sem `version`: a action lê `packageManager` do package.json, então CI e
      # local usam exatamente a mesma pnpm. Com `version: 11` (flutuante) o CI
      # quebrou sozinho quando o pnpm 11.17 passou a falhar em builds ignorados.
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Install Playwright Browsers
        run: pnpm exec playwright install chromium --with-deps
      - name: Run Playwright tests
        run: pnpm exec playwright test
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
